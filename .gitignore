# 依赖与构建产物
node_modules/
.output/
.wxt/
dist/
*.zip
*.crx
*.pem
*.log
.DS_Store

# 环境与密钥
.env
.env.*
!.env.example

# 编辑器
.idea/
.vscode/*
!.vscode/extensions.json

# 本地 Whisper 运行时（C 路按需安装的第三方产物，约 13MB）
# 用 scripts/install-whisper-runtime.mjs 生成；
# 不提交是为了让不用 C 路的用户拿到的包体保持 ~13MB
public/whisper/

# 分析原料（独立归档在 ../reverse-immersive-translate/）
reverse/

# 临时验证脚本（esbuild 转译后的断言脚本）
.scratch/
